apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-service
  namespace: datacenter
spec:
      imagePullSecrets:
        - name: ghcr-secret
  replicas: 1
  selector:
    matchLabels:
      app: ml-service
  template:
    metadata:
      labels:
        app: ml-service
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: ml
          image: datacenter-ml-service:latest
          ports:
            - containerPort: 8000